class Company {
  constructor() {
    this.department = [];
    this.addEmployee = this.addEmployee(name, salary, position, department)  {
      const isInputValid = (name && salary && position && department) || salary < 0;
    },
    this.bestDepartment = bestDepartment() {

    }
  }
}
let proxiad = new Company();
console.log(proxiad);




proxiad.addEmployee("Stanimir", 2000, "engineer", "Construction");
console.log(proxiad);
